{
  "name": "@sip/worker",
  "private": true,
  "version": "0.1.0",
  "description": "SIP volume worker: observes every buy and sell a bound trading wallet makes anywhere on Robinhood Chain, attests the volume, and pulls the skim into the user's vault through the wallet's Privy seat. Dry run by default.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "tick": "tsx bin/worker.mts tick",
    "worker": "tsx bin/worker.mts run"
  },
  "dependencies": {
    "@nuvem/contracts-artifacts": "workspace:*",
    "@privy-io/node": "^0.28.0",
    "pg": "^8.23.0",
    "viem": "2.55.8"
  },
  "devDependencies": {
    "@types/node": "22.20.0",
    "@types/pg": "^8.21.0",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
