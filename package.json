{
  "name": "sip",
  "private": true,
  "version": "0.1.0",
  "description": "SaverFi \u2014 a pension you build one trade at a time, on Solana. A slice of every linked wallet's trading, measured as volume or as realized profit, is settled into the user's vault and invested.",
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": "22.14.0"
  },
  "scripts": {
    "build": "pnpm --dir packages/website-oficial build",
    "test": "pnpm --dir packages/solana-log test && pnpm --dir packages/solana-core test && pnpm --dir packages/solana-keeper test && pnpm --dir packages/website-oficial test",
    "test:keeper": "pnpm --dir packages/solana-keeper test",
    "typecheck": "pnpm --dir packages/solana-log typecheck && pnpm --dir packages/solana-core typecheck && pnpm --dir packages/solana-keeper typecheck && pnpm --dir packages/website-oficial typecheck",
    "dev": "pnpm --dir packages/website-oficial dev"
  },
  "devDependencies": {
    "tsx": "4.23.1"
  }
}
